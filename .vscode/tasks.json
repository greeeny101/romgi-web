{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Start Postgres + Redis",
			"type": "shell",
			"command": "docker compose up -d postgres redis",
			"options": { "cwd": "${workspaceFolder}" },
			"problemMatcher": [],
			"presentation": { "reveal": "silent", "panel": "shared" }
		},
		{
			"label": "Start Postgres + Redis + qBittorrent",
			"detail": "Needed for torrent-related work — the debug launch configs only start Postgres + Redis by default.",
			"type": "shell",
			"command": "docker compose up -d postgres redis qbittorrent",
			"options": { "cwd": "${workspaceFolder}" },
			"problemMatcher": [],
			"presentation": { "reveal": "silent", "panel": "shared" }
		}
	]
}
